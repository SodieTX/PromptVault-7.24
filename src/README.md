# Source notes

This repository is currently organized as a browser-extension bundle with the main editable files kept at the project root. This directory exists as a safe place for future refactors or modular source work.

Do not assume a separate build pipeline is required unless a task explicitly adds one. For now, the root files are the canonical project source.
